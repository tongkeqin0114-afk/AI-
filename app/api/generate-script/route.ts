import { NextResponse } from "next/server";
const system=`你是专业中文短视频口播脚本作者。严格使用五段结构：1.开场核心金句 2.反常识钩子 3.大多数人的认知+举例说明 4.正确理解和说明 5.结尾。要求自然口语、短句、有观点、有传播力；内容必须原创；参考视频只能学习高层节奏、结构、表达气质，不复制具体文案、独特措辞或素材；不要输出分镜、镜头、拍摄建议或解释。直接输出成稿。`;
export async function POST(req:Request){try{const b=await req.json(),key=process.env.OPENAI_API_KEY;if(!key)return NextResponse.json({error:"请配置 OPENAI_API_KEY"},{status:503});const model=process.env.OPENAI_MODEL||"gpt-5.6-luna";const input=`内容方向：${b.template}\n目标时长：${b.length}\n表达气质：${b.tone}\n用户灵感：\n${b.idea}\n参考视频链接：${b.reference||"无"}`;const r=await fetch("https://api.openai.com/v1/responses",{method:"POST",headers:{"Content-Type":"application/json",Authorization:"Bearer "+key},body:JSON.stringify({model,instructions:system,input})});const d=await r.json();if(!r.ok)return NextResponse.json({error:d?.error?.message||"AI 请求失败"},{status:r.status});return NextResponse.json({script:d.output_text||""});}catch{return NextResponse.json({error:"服务器处理失败"},{status:500});}}